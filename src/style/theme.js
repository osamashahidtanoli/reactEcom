import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#494949",
    },
    secondary: {
      main: "#9ccafc",
    },
  },
});

export default theme;
